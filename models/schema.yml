Version: 2

#Models:
 # - name: dim_customers
 #   description: "Customer dimension."
  #  columns:
   #   - name: customer_id
    #    tests: [not_null, unique]
     # - name: customer_status
      #   - accepted_values:
        #      values: ["ACTIVE","INACTIVE"]


sources:
 - dfs_dw
   schema: 
   - silver
   tables:
     - table_name
     - table_name

sources:
 - dfs_dw
   schema: 
   - gold
   tables:
     - table_name
     - table_name
