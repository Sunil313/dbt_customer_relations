name: my_dbt_project
version: 1.0.0
config-version: 2

model-paths: ["models"]
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  bfs_dw:

    staging:
      +schema: staging
      +materialized: view
    
    staging_core_banking:
      +schema: staging_cb
      +materialized: view    

    staging_payments:
      +schema: staging_payments
      +materialized: view   

    staging_crm:
      +schema: staging_crm
      +materialized: view   
    
    staging_crm:
      +schema: staging_crm
      +materialized: view   

    staging_crm:
      +schema: staging_crm
      +materialized: view   

    marts:
      +schema: analytics
      +materialized: table

    snapshots:
      +schema: snapshots
      +materialized: view   
     
    bfs_dw:
      +schema: bfs_dw
      +materialized: view  

    risk:
      +schema: risk
      +materialized: table

    operations:
      +schema: operations
      +materialized: table
     
    reporting:
      +schema: reporting
      +materialized: table
     
    intermediate:
      +schema: intermediate
      +materialized: table
    
    seeds:
      +schema: dw_raw
      +materialized: table

      
target-path: "target"  
clean-targets:         
  - "target"
  - "dbt_packages"
